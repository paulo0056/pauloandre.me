name: deploy
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: "12"
      - name: setup git auth
        env:
          email: "pauloandre.dev@gmail.com"
          username: "paulo0056"
          access_token: "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"
        run: |
          git config --global credential.helper store
          echo "https://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:x-oauth-basic@github.com" > ~/.git-credentials
          git config --global user.email $email
          git config --global user.name $username
      - name: deploy to gh-pages
        run: |
          npm i
          NODE_ENV=production npm run build
          cd dist
          git init
          git add -A
          git commit -m "🚀 Deploy "
          git push -f "https://github.com/pauloandre.me.git" master:gh-pages
